import * as React from 'react'
import { useEffect, useRef, useCallback, useState } from 'react'
import { setImageSize } from '../../utils/format'

import { Control, Operator, PlayBarWrapper, PlayerInfo } from './stye';